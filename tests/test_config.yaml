tiktoken_encoding: "gpt2"

llm:
  provider: "openai"
  model: "gpt-3.5-turbo"
  temperature: 0.0

vector_store:
  provider: "faiss"

embedding_model:
  provider: "openai"
  model: "text-embedding-ada-002"
